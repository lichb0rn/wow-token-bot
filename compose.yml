name: wow-token-bot

services:
  application:
    container_name: wow-token-bot
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/home/node/bot
    env_file:
      - .env
    tty: true
